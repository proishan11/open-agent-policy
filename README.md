# open-agent-policy
Open Agent Policy is the open-source policy and authorization layer for AI agents: agent identity, scoped delegation, resource policies, approval gates, runtime enforcement, and auditable zero-trust access.
